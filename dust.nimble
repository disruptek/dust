version = "0.0.1"
author = "disruptek"
description = "dust: unattended syntax truncation"
license = "MIT"
requires "nim >= 1.0.4"
requires "https://github.com/narimiran/itertools < 1.0.0"

bin = @["dust"]
